package comparingobjects;

import java.util.Comparator;

/**
 * java.util.Comparator 接口
 *
 * A comparison function, which imposes a total ordering on some collection of objects.
 * 一种 比较函数, 为某些 对象集合 强加一个总排序
 * Comparators can be passed to a sort method (such as Collections.sort or Arrays.sort)
 * to allow precise control over the sort order.
 * 比较器 可以传递给 排序方法 (例如 Collections.sort 或 Arrays.sort), 以允许精准的控制排序顺序
 * Comparators can also be used to control the order of certain data structures (such as sorted sets or sorted maps),
 * or to provide an ordering for collections of objects that don't have a natural ordering.
 * 比较器 还可以被用于控制 特定的数据结构(例如 排序的sets 或 排序的maps) 的顺序,
 * 或者为 没有自然排序的 对象集合 提供一个排序
 *
 * @author RABBIT2002
 * @date 2020/12/28
 */
public class ComparatorClass {
    public static void main(String[] args) {
    }
}
